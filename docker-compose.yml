services:
  db:
    image: postgres:16
    restart: unless-stopped
    environment:
      POSTGRES_USER: homefix
      POSTGRES_PASSWORD: homefix
      POSTGRES_DB: homefix
    ports:
      - "5432:5432"
    volumes:
      - homefix_pgdata:/var/lib/postgresql/data

volumes:
  homefix_pgdata:
